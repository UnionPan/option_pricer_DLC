# Cloud Build configuration for frontend deployment to App Engine
steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/options-desk-frontend:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/options-desk-frontend:latest'
      - '-f'
      - 'deployment/docker/frontend/Dockerfile'
      - '.'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/options-desk-frontend:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/options-desk-frontend:latest'

  # Deploy to App Engine
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'app'
      - 'deploy'
      - 'deployment/appengine/frontend.yaml'
      - '--image-url'
      - 'gcr.io/$PROJECT_ID/options-desk-frontend:$COMMIT_SHA'
      - '--quiet'

images:
  - 'gcr.io/$PROJECT_ID/options-desk-frontend:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/options-desk-frontend:latest'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
