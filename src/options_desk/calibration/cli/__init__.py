"""Console entry points for the calibration pipeline.

Registered as project scripts in pyproject.toml:
    calibrate-universe  -> calibrate:main
    build-universes     -> build_universes:main
    train-npe-heston    -> train_npe:main

The thin files under ``scripts/`` re-export these mains for
``python scripts/<name>.py`` usage.
"""
