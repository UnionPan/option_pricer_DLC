name: Deploy Backend to App Engine

on:
  push:
    branches: [main, feature/larx]
    paths:
      - 'backend/**'
      - 'src/**'
      - 'deployment/docker/backend/**'
      - 'deployment/cloudbuild/backend-appengine.yaml'
      - 'deployment/appengine/backend.yaml'

  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_SA_PROJECT_ID_LARX }}
  SERVICE_NAME: backend

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_LARX }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v1

      - name: Submit build to Cloud Build
        run: |
          gcloud builds submit \
            --project=$PROJECT_ID \
            --config=deployment/cloudbuild/backend-appengine.yaml \
            --substitutions=COMMIT_SHA=${{ github.sha }} \
            .
